import { NumberChangerPipe } from './number-changer.pipe';

describe('NumberChangerPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberChangerPipe();
    expect(pipe).toBeTruthy();
  });
});
